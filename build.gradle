plugins{
    id "com.avast.gradle.docker-compose" version "0.9.4"
}

apply plugin: 'base'

// Apply the java plugin to add support for Java
apply plugin: 'idea'

// Applying repositories
apply from: 'configs/repositories.gradle'

// Applying docker config
apply from: 'configs/docker.gradle'

dependencies {
}


ext{
    appName = 'python-ms-template'
    groupName = "in.ms92.mcloud"
}